import 'package:flutter/material.dart';
class AuspiciadorPage extends StatelessWidget {
   
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:AppBar(
        title: Text('Auspiciador Page'),
      ) ,
      body: 
        Text('data')
      );
  }
}